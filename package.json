{
  "name": "agents_fleet",
  "private": true,
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:one": "sh ./scripts/dev",
    "check": "sh ./scripts/check",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  }
}
